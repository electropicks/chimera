{
  "name": "@creature/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit --pretty false --allowImportingTsExtensions -p tsconfig.json && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit --pretty false --allowImportingTsExtensions -p tsconfig.json"
  },
  "dependencies": {
    "@creature/sim-core": "workspace:*",
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
