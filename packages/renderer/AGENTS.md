# @creature/renderer

This workspace is reserved for rendering-facing abstractions and presentation adapters. Keep changes focused on this responsibility and avoid adding runtime dependencies unless the package plan requires them.
