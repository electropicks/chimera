# @creature/headless-sim

This workspace is reserved for headless simulation tooling for non-visual runs. Keep changes focused on this responsibility and avoid adding runtime dependencies unless the package plan requires them.
