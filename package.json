{
  "name": "chimera",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false --allowImportingTsExtensions --allowJs --checkJs",
    "test": "vitest run --passWithNoTests --configLoader runner",
    "lint": "biome check . && pnpm lint:sim-core-determinism && pnpm lint:deps",
    "lint:sim-core-determinism": "node tools/check-sim-core-determinism-cli.mjs",
    "lint:deps": "depcruise --config .dependency-cruiser.cjs \"packages/**/src/**/*.ts\" \"tools/**/src/**/*.ts\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/node": "^22.19.19",
    "dependency-cruiser": "^17.4.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
